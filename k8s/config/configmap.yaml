apiVersion: v1
kind: ConfigMap
metadata:
  name: shop-config
  namespace: shop
data:
  application.conf: |
    APP_NAME=shopshere-order-api
    ENVIRONMENT=production
    LOG_LEVEL=INFO
    DATABASE_HOST=postgres
    DATABASE_PORT=5432
    REDIS_HOST=redis
    REDIS_PORT-6379