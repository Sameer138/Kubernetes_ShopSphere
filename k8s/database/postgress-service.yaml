# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-service
#   namespace: shop
# spec:
#   selector:
#     app: shop-app
#   ports:
#     - protocol: TCP
#       targetPort: 5432
#       port: 5432
#   type: ClusterIP
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: shopsphere
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP